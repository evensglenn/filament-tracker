{
  "entities": {
    "Filament": {
      "title": "Filament",
      "description": "A 3D printing filament spool in the inventory",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Document ID" },
        "uid": { "type": "string", "description": "Owner User ID" },
        "brand": { "type": "string" },
        "type": { "type": "string" },
        "colorName": { "type": "string" },
        "colorHex": { "type": "string" },
        "quantity": { "type": "number" },
        "notes": { "type": "string" },
        "lastUsed": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "brand", "type", "colorName", "colorHex", "quantity"]
    },
    "Share": {
      "title": "Share",
      "description": "List of emails that have access to a user's inventory",
      "type": "object",
      "properties": {
        "emails": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["emails"]
    }
  },
  "firestore": {
    "/filaments/{filamentId}": {
      "schema": "Filament",
      "description": "User's filament inventory"
    },
    "/shares/{ownerUid}": {
      "schema": "Share",
      "description": "Sharing settings for a user"
    }
  }
}
